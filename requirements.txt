# Reco Love: Gold Beach 简体中文化工具链依赖
# 仅“从源码重建”需要；应用 xdelta 补丁不需要 Python 环境。
#
# 核心 CPK/ADV 解析与重建（parse_cpk / crilayla / adv_parser / adv_builder /
# rebuild_cpk / build_xdelta_v100）均为 Python 标准库实现。
# 以下依赖用于字库渲染、贴图处理与 Excel 校对等辅助脚本。

numpy>=1.21
Pillow>=9.0
freetype-py>=2.3
openpyxl>=3.0
